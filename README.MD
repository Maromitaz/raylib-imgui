# Description

As the name suggests. This project unifies raylib with imgui.

It also serves as a template project to start working with both.

At the moment, it only works with Windows on x86 architecture on 64 bit
